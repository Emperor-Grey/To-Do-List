<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Experiment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <!-- Applying the made theme here -->
        <item name="bottomSheetDialogTheme">@style/AppModelTheme</item>

    </style>

    <style name="Theme.ToDoList" parent="Base.Theme.Experiment" />

    <!--  Bottom sheet Dialogue theme -->
    <style name="AppModelTheme" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModelStyle</item>
    </style>

    <!-- Bottom sheet Style -->
    <style name="AppModelStyle" parent="Widget.Material3.BottomSheet.Modal">
        <item name="android:background">@drawable/top_round</item>
        <item name="behavior_peekHeight">370dp</item>
    </style>
</resources>